name: barang_kost
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you plan to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.0.0
  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0  # versi 5.0.0a tidak valid. Gunakan versi yang tersedia.

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/data/
